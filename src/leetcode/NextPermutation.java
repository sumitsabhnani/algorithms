package leetcode;

public class NextPermutation {

    public int[] nextPermutation(int[] nums) {
        for (int i = nums.length - 1; i > 0; i--) {
            if(nums[i] > nums[i-1]) {
                int min = nums[i-1];
                
            }
        }

        return nums;
    }
}
